import type {Metadata} from 'next';import './globals.css';export const metadata:Metadata={title:'Ajvar — Your family’s kitchen',description:'Keep your favourite recipes, share them with your kitchen, and discover something delicious.',icons:{icon:'/favicon.svg',shortcut:'/favicon.svg'}};export default function RootLayout({children}:Readonly<{children:React.ReactNode}>){return <html lang="en"><body>{children}</body></html>}